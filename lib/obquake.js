'use strict';

const {databaseMap} = require('../config/config');

class obquake {
    constructor(database, name, password, options){
    let config;

    this.config = {database, name, password};
        //TODO: start connecting databases etc. here
    }
}

module.exports = obquake;