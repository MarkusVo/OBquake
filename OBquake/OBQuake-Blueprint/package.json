{
  "name": "dbmigrationblueprint",
  "version": "1.0.0",
  "description": "Testproject for database Migration",
  "main": "index.js",
  "scripts": {
    "db:start": "docker-compose up -d",
    "db:stop": "docker-compose down",
    "db:migration:up": "db-migrate up",
    "db:migration:down": "db-migrate up",
    "db:migration:redo": "npm run db:migration:down && npm run db:migration:up",
    "db:migration:create": "db-migrate create"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "db-migrate": "^0.11.5",
    "db-migrate-pg": "^0.5.0",
    "dotenv": "^7.0.0"
  }
}
